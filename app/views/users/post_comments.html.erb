<div class="row">
  <div class="col-md-12">
    <table class="table table-hover">
      <thead>
        <tr>
          <th>Comment名稱</th>
        </tr>
      </thead>
      <tbody>
        <% @comments.each do |comment| %>
            <tr>
              <td><%= link_to comment.content, topic_path(comment.topic_id) %></td>
            </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
